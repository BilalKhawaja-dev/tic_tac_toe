{
  "name": "pipeline-tests",
  "version": "1.0.0",
  "description": "Tests for CI/CD pipeline configuration",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "aws-sdk": "^2.1400.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "../../infrastructure/terraform/modules/cicd/lambda/**/*.js"
    ]
  }
}
